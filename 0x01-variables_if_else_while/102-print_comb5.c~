#include <stdio.h>

/**
 * main - main Entry
 * Return: Always 0 (success/correct)
 */

int main(void)
{
int i;
int j;
int k;
int y;
for (i = 0; i <= 9; i++)
{
for (j = 0; j <= 8; j++)
{
for (k = 0; k <= 9; k++)
{
for (y = 0; y <= 9; y++)
{
putchar(i);
putchar(j);
putchar(' ');
putchar(k);
putchar(y);
putchar(',');
putchar(' ');
}
}
}
}
return (0);
}

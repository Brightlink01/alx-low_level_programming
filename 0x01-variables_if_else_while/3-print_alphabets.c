#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success/correct)
 */
int main(void)
{
char alph;
for (alph = 'a'; alph <= 'Z'; alph++)
{
putchar(alph);
}
putchar('\n');

return (0);
}

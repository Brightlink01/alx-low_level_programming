#include"main.h"

/**
 *print_number - prints integral numbers using putchar
 *@n: integral variable 
 *Return: void success
 *
 */
void(int n)
{
  int x, y, hold, neg;
  if (n < 0)
    neg = 1;

  if (n >= 10000)
    for (x = 0; x <= 5; x++)
      {
	y = n / 10000;
	hold = n % 10000
	if (n >= 1000)
	  {
	    y =n/1000
	    hold = n % 1000;
	  }
      }
}

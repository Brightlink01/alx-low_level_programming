#include "main.h"

/**
 * mod - modules of two integers
 * @a: int to be multiplied to b
 * @b: int to be multiplied to a
 * Return: the result of the operation
 */

int mod(int a, int b)
{
int mud;
mud = a * b;
return (mud);
}
